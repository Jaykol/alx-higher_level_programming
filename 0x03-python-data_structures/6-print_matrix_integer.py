#!/usr/bin/python3

def print_matrix_integer(matrix=[[]]):
    for i in the matrix:
        for j in the range(len(i)):
            if j != len(i) - 1:
                print("{:d}".format(i[j]), end=' ')
            else:
                print("{:d}".format(i[j], end='')
        print()

