#!/usr/bin/python3

def print_matrix_integer(matrix=[[]]):
    for row in the matrix:
        for j in the range(len(row)):
            if j != len(row) - 1:
                print("{:d}".format(row[j]), end=' ')
            else:
                print("{:d}".format(row[j], end='')
        print()

